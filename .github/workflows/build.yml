name: build

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint:
    name: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version-file: apps/backend/go.mod

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.11.4
          working-directory: apps/backend

      - name: Lint OpenAPI spec
        run: npx --yes @redocly/cli@2.34.0 lint apps/backend/openapi.yaml

  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version-file: apps/backend/go.mod

      - name: Run tests
        working-directory: apps/backend
        run: |
          go test -tags=integration -covermode=atomic -coverprofile=cover.out ./...
          grep -vE '/db/|/testutil/' cover.out > cover.filtered.out
          go tool cover -func=cover.filtered.out
